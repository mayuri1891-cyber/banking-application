server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://auth-service:80
          predicates:
            - Path=/auth/**

management:
  endpoints:
    web:
      exposure:
        include: "*"